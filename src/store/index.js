import Vue from 'vue'
import Vuex from 'vuex'

import { stepper } from "./stepper";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    stepper,
  }
})
